Description:
    Generates migration, models and controller with views for the audit-log.
    ./script/generate auditable Controller SessionModel RequestModel ChangeModel

Example:
    script/generate auditable audits auditable_session auditable_request auditable_change
    
 
